# 実装タスク: csv-chart-short-video

## タスク一覧

---

- [x] 1. プロジェクト環境のセットアップ
- [x] 1.1 ディレクトリ構成と依存ライブラリの設定 <!-- Closes #1 -->
  - `requirements.txt` に必要なライブラリ（bar-chart-race, matplotlib, moviepy, pandas, anthropic, imageio-ffmpeg）とバージョンを定義する
  - `src/` 配下に各コンポーネント用モジュールファイルを作成する（中身は空でよい）
  - `output/` および `output/tmp/` ディレクトリを `.gitkeep` で確保する
  - Python 3.10 以上で動作する `main.py` エントリーポイントを作成する
  - 起動時に `ANTHROPIC_API_KEY` 環境変数と FFmpeg の存在を確認し、未設定の場合はセットアップ手順を表示して終了する処理を実装する
  - _Requirements: 7.5_

---

- [x] 2. CSVスキャナーの実装
- [x] 2.1 カレントディレクトリのCSVファイル自動検出 <!-- Closes #2 -->
  - カレントディレクトリ内の `*.csv` ファイルを列挙して番号付きリストで表示する機能を実装する
  - CSVが1ファイルのみの場合は自動選択してユーザーに通知する
  - CSVが見つからない場合はファイルの置き場所と形式を案内するエラーメッセージを表示する
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2.2 CSVファイルのロードとメタデータ抽出 <!-- Closes #3 -->
  - UTF-8 および Shift-JIS エンコーディングを自動検出してCSVを読み込む
  - 列名・行数・データ型・日時列有無・サンプル行（先頭5行）を含むメタデータを抽出する
  - 読み込み成功後にカラム名・行数・データ型のサマリーをコンソールに表示する
  - 数値データが含まれない列をスキップしてユーザーに警告を表示する
  - _Requirements: 1.4, 1.5_

---

- [x] 3. AIデータ分析器の実装
- [x] 3.1 Claude APIを使ったチャートタイプ自動推薦 <!-- Closes #4 -->
  - CSVメタデータ（列情報・行数・サンプル行）を構造化プロンプトに変換して Claude Haiku に送信する
  - `bar_race` / `animated_bar` / `animated_line` の3種類からチャートタイプを推薦させ、推薦理由・使用列情報をJSON形式で受け取る
  - APIレスポンスのJSONパースに失敗した場合は、データ構造に基づくルールベースのフォールバック推薦に切り替える
  - 推薦チャートタイプと理由をコンソールに表示し、ユーザーが変更できる選択肢を提供する
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

---

- [x] 4. (P) テーママネージャーの実装
- [x] 4.1 カラーテーマの定義と適用ロジック <!-- Closes #5 -->
  - ダーク（YouTube向け）・パステル（TikTok向け）・デフォルトの3テーマを不変の定数として定義する
  - 各テーマの背景色・バーカラーリスト・テキスト色・フォントファミリーを設定する
  - テーマ選択プロンプトで用途説明付きの番号選択UIを実装する
  - 選択がスキップされた場合はデフォルトテーマを自動適用する
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

---

- [x] 5. チャートジェネレーターの実装
- [x] 5.1 バーレースチャートアニメーションの生成 <!-- Closes #6 -->
  - `bar_chart_race` ライブラリを使って時系列データから順位変動アニメーションを生成する
  - `figsize=(10.8, 19.2)`, `dpi=100` を固定して縦型1080×1920pxで出力する
  - テーマ設定（背景色・バーカラー・テキスト色）をチャートに適用する
  - デフォルト30秒・30fpsで一時MP4ファイルを生成する
  - `steps_per_period` を適切に設定して月間データを滑らかに補間する
  - 生成中の進捗率をコールバックでオーケストレーターに通知する
  - _Requirements: 4.1, 4.2, 4.3, 4.5, 4.6_

- [x] 5.2 棒グラフ・折れ線グラフアニメーションの生成 <!-- Closes #7 -->
  - `matplotlib FuncAnimation` でカテゴリ比較の棒グラフアニメーションを実装する（バーが下から伸びる）
  - 折れ線グラフは左から右へ線が描かれるアニメーションを実装する
  - バーレースと同様に `figsize=(10.8, 19.2)`, `dpi=100` で縦型フォーマットを統一する
  - データポイント間を `numpy.linspace` で補間して滑らかなフレームを生成する
  - _Requirements: 4.1, 4.3, 4.5_

---

- [x] 6. (P) イベントアノテーションの実装
- [x] 6.1 events.csvの読み込みとフレーム番号への変換 <!-- Closes #8 -->
  - カレントディレクトリの `events.csv`（`period`, `category`, `text` 列）を自動検出して読み込む
  - `events.csv` が存在しない場合はアノテーションなしで処理を続行する
  - イベントの `period` 値をメインデータの時系列と照合してフレーム番号に変換する
  - 検出したイベント一覧と対応フレーム番号をコンソールに表示する
  - _Requirements: 6.1, 6.5, 7.6_

- [x] 6.2 吹き出しアノテーションの描画とフェード処理 <!-- Closes #9 -->
  - `ax.annotate` の `arrowprops` を使って対象バーの右端に矢印付き吹き出しを描画する
  - 吹き出しのデザインを半透明の角丸ボックスで実装する
  - 該当フレームでフェードイン（alpha 0→1）、約30フレーム後にフェードアウト（alpha 1→0）するアニメーションを実装する
  - テキストが縦型フォーマット（幅1080px）に収まるよう自動折り返し処理を実装する
  - _Requirements: 6.2, 6.3, 6.4, 6.6_

---

- [x] 7. ビデオレンダラーの実装
- [x] 7.1 フェードイン・アウト効果の適用 <!-- Closes #10 -->
  - `moviepy` を使ってチャートジェネレーターが出力した一時MP4の冒頭・末尾にフェードイン・アウト（各0.5秒）を適用する
  - フェード処理後に `output/tmp/` の一時ファイルを削除する
  - _Requirements: 4.4_

- [x] 7.2 MP4最終出力とファイルサイズ最適化 <!-- Closes #11 -->
  - `output/{CSVファイル名}_{YYYYMMDD}.mp4` の形式でファイル名を自動生成して保存する
  - `output/` ディレクトリが存在しない場合は自動作成する
  - H.264コーデック（`libx264`）・ビットレート3000kbpsでエンコードする
  - 出力ファイルが20MBを超える場合はビットレートを自動調整して再エンコードする
  - 完了後に出力ファイルパス・ファイルサイズ・動画尺をコンソールに表示する
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_

---

- [x] 8. VideoAppオーケストレーターの実装
- [x] 8.1 インタラクティブCLIフローの実装 <!-- Closes #12 -->
  - CSV選択 → AI分析・チャートタイプ確認 → テーマ選択 → 動画生成 → 完了表示の順でフローを実装する
  - 各ステップで番号付き選択肢を表示し、スキップ時はデフォルト値で続行できるUIを実装する
  - 無効な入力値に対してエラーメッセージと有効範囲を表示して再入力を促す
  - _Requirements: 7.2, 7.3, 7.4_

- [x] 8.2 全コンポーネントの統合と進捗表示 <!-- Closes #13 -->
  - CSVScanner・DataAnalyzer・ThemeManager・ChartGenerator・EventAnnotator・VideoRenderer を順次呼び出す処理をつなぎ合わせる
  - 動画生成中の進捗状況をプログレスバー形式（`████░░ 60%`）でリアルタイム表示する
  - カスタム例外（`CSVScanError`, `AnalysisError`, `RenderingError`）をキャッチしてユーザーフレンドリーなエラーメッセージと解決策を表示する
  - _Requirements: 4.6, 7.1_

---

- [x] 9. (P) Claude Codeスキルの定義
- [x] 9.1 スキルファイルの作成と動作確認 <!-- Closes #14 -->
  - `.claude/skills/create-chart-video/SKILL.md` を作成し、フロントマター（`name`, `description`, `allowed-tools`）を定義する
  - スキル実行時に `main.py` を呼び出すBashコマンドを記述する
  - Python環境・ライブラリが未インストールの場合に `pip install -r requirements.txt` のセットアップ手順を案内するチェック処理を追加する
  - _Requirements: 7.1, 7.2, 7.5_

---

- [x] 10. 統合テストとE2E検証
- [x] 10.1 コンポーネント統合テスト <!-- Closes #15 -->
  - CSVScanner → DataAnalyzer の流れで実CSV読み込みから推薦取得までを検証する
  - DataAnalyzer → ChartGenerator の流れで推薦結果から一時MP4生成までを検証する
  - ChartGenerator → EventAnnotator → VideoRenderer の流れで吹き出し付き最終MP4出力を検証する
  - _Requirements: 1.4, 2.1, 4.1, 5.1, 6.1_

- [x] 10.2 E2Eテスト（サンプルCSVでの動画生成確認） <!-- Closes #16 -->
  - 月次ランキングデータのサンプルCSVと events.csv を用意してスキル実行からMP4生成までの全フローを検証する
  - 出力MP4が縦型1080×1920px・30fps・20MB以下であることを確認する
  - bar_race / animated_bar / animated_line の各チャートタイプで正常生成を確認する
  - _Requirements: 4.3, 4.5, 5.5, 6.2_

- [x] 10.3 ユニットテストの整備（オプション・MVP後） <!-- Closes #17 -->
  - CSVScanner の UTF-8/Shift-JIS 自動検出・空ディレクトリ・不正CSV各ケースをユニットテストで網羅する
  - DataAnalyzer のモックAPI応答パース・フォールバック動作をユニットテストで検証する
  - ThemeManager の全テーマ取得・無効名エラーをユニットテストで検証する
  - VideoRenderer のファイル名生成・出力ディレクトリ自動作成をユニットテストで検証する
  - _Requirements: 1.3, 1.5, 2.5, 3.1, 5.2_
